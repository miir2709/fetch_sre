# fetch_sre

Take Home Assessment for Fetch SRE Position. I used popular libraries, which are well maintained. 

pip install [ library ]


 I build my code using python3 health_check.py config.yaml, since .py and .yaml files are in the same directory. 
